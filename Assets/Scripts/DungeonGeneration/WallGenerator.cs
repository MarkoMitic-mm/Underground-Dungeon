using System.Collections.Generic;
using UnityEngine;

public class WallGenerator
{
    public HashSet<Vector2Int> CreateWalls(
        HashSet<Vector2Int> floorPositions)
    {
        HashSet<Vector2Int> walls = new();

        return walls;
    }
}
